username = "foo"
monitor_all_users = false
monitor_all_libraries = true

[general]
remove_watched_enabled = true
remove_watched_days = 60
remove_unwatched_enabled = false
remove_unwatched_days = 365
dry_run = true

[jellyfin]
base_url = "http://localhost:8096"
api_key = "api-key-foo"

[radarr]
base_url = "http://localhost:7878"
api_key = "api-key-foo"
tags_to_keep = ["keep"]
retention_period = "2d"

[sonarr]
base_url = "http://localhost:8989"
api_key = "api-key-foo"
tags_to_keep = ["keep"]
retention_period = "1w"

[download_clients.qbittorrent]
base_url = "http://localhost:8080"
username = "admin"
password = "adminadmin"

[download_clients.deluge]
base_url = "http://localhost:8112"
password = "qwerty"
